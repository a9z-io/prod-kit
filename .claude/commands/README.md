# Optional overlay: `.claude/commands/*`

Prod‑Kit can include agent guidance documents under `.claude/commands/`.

This overlay is optional; apply it only if you want these docs in your repository.

